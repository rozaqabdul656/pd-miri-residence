import {getFacilities} from "@/lib/data"; import {Crumb,SafeImage,SectionTitle} from "@/components/common"; import * as Icons from "lucide-react"; export const metadata={title:"Fasilitas"}; export default function Facilities(){const f=getFacilities();return <section className="shell section"><Crumb current="Fasilitas"/><SectionTitle eyebrow="Lingkungan" title="Fasilitas Pondok Miri Residence" copy="Data fasilitas berikut merupakan placeholder yang dapat diperbarui melalui JSON."/><div className="mt-10 grid gap-6 md:grid-cols-3">{f.map(x=>{const Icon=(Icons as any)[x.icon==='route'?'Route':x.icon==='trees'?'Trees':'ShieldCheck'];return <article className="card" key={x.id}><div className="relative aspect-[16/10]"><SafeImage src={x.image} alt={x.name}/></div><div className="p-6"><Icon className="text-green"/><h2 className="mt-4 text-xl font-semibold">{x.name}</h2><p className="mt-2 leading-7 text-slate">{x.description}</p></div></article>})}</div></section>}
