import type {MetadataRoute} from "next"; import {getActivities} from "@/lib/data"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://pondok-miri-residence.vercel.app";return ["","/tentang","/kegiatan","/galeri","/fasilitas","/pengumuman","/lokasi","/kontak","/pengurus"].map(url=>({url:`${base}${url}`,lastModified:new Date()})).concat(getActivities().map(a=>({url:`${base}/kegiatan/${a.slug}`,lastModified:new Date(a.date)})))}
