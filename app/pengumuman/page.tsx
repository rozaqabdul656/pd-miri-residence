import {getAnnouncements} from "@/lib/data"; import {formatDate} from "@/lib/utils"; import {Crumb,Empty,SectionTitle} from "@/components/common"; export const metadata={title:"Pengumuman"}; export default function Announcements(){const items=getAnnouncements();return <section className="shell section"><Crumb current="Pengumuman"/><SectionTitle eyebrow="Informasi warga" title="Pengumuman terbaru"/>{items.length?<div className="mt-10 space-y-4">{items.map(a=><article className="rounded-2xl border border-forest/10 p-6" key={a.id}>{a.important&&<span className="rounded-full bg-forest px-3 py-1 text-xs font-bold text-paper">Penting</span>}<p className="mt-3 text-sm text-slate">{formatDate(a.date)}</p><h2 className="mt-1 text-xl font-semibold">{a.title}</h2><p className="mt-2 leading-7 text-slate">{a.description}</p></article>)}</div>:<div className="mt-8"><Empty>Belum ada pengumuman aktif.</Empty></div>}</section>}
