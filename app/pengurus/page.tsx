import {getManagement} from "@/lib/data"; import {Crumb,Empty,SafeImage,SectionTitle} from "@/components/common"; export const metadata={title:"Pengurus"}; export default function Management(){const members=getManagement();return <section className="shell section"><Crumb current="Pengurus"/><SectionTitle eyebrow="Komunitas" title="Struktur pengurus"/>{members.length?<div className="mt-10 grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{members.map(m=><article className="card p-5" key={`${m.name}-${m.role}`}><div className="relative aspect-square overflow-hidden rounded-xl"><SafeImage src={m.photo} alt={m.name}/></div><h2 className="mt-4 font-semibold">{m.name}</h2><p className="text-sm text-slate">{m.role}</p></article>)}</div>:<div className="mt-8"><Empty>Data struktur pengurus belum tersedia.</Empty></div>}</section>}
