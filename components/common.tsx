import Link from "next/link"; import Image from "next/image"; import {ImageOff,ArrowRight,ChevronRight} from "lucide-react"; import type {ReactNode} from "react";
export function SectionTitle({eyebrow,title,copy}:{eyebrow:string;title:string;copy?:string}){return <div className="max-w-2xl"><p className="eyebrow">{eyebrow}</p><h2 className="mt-3 text-3xl font-semibold tracking-tight text-forest sm:text-4xl">{title}</h2>{copy&&<p className="mt-4 text-lg leading-8 text-slate">{copy}</p>}</div>}
export function SafeImage({src,alt,className,priority=false}:{src?:string;alt:string;className?:string;priority?:boolean}){if(!src)return <div className={`grid place-items-center bg-sage ${className}`}><ImageOff aria-hidden="true"/></div>;return <Image src={src} alt={alt} fill className={`object-cover ${className||""}`} sizes="(max-width:768px) 100vw, 50vw" priority={priority}/>}
export function Empty({children}:{children:ReactNode}){return <div className="rounded-2xl border border-dashed border-moss/30 bg-cream p-10 text-center text-slate">{children}</div>}
export function Crumb({current}:{current:string}){return <nav aria-label="Breadcrumb" className="mb-8 flex items-center gap-2 text-sm text-slate"><Link href="/">Beranda</Link><ChevronRight size={14}/><span className="text-forest">{current}</span></nav>}
export function More({href,label="Lihat selengkapnya"}:{href:string;label?:string}){return <Link className="inline-flex items-center gap-2 font-medium text-green transition hover:gap-3" href={href}>{label}<ArrowRight size={16}/></Link>}
