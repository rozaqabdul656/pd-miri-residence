import Link from "next/link"; import {CalendarDays,ArrowUpRight} from "lucide-react"; import type {Activity} from "@/types"; import {SafeImage} from "@/components/common"; import {formatDate} from "@/lib/utils";
export function ActivityCard({activity}:{activity:Activity}){return <article className="card group"><div className="relative aspect-[16/10] overflow-hidden"><SafeImage src={activity.cover} alt={activity.title}/></div><div className="p-5"><div className="flex items-center justify-between gap-3"><span className="rounded-full bg-sage px-3 py-1 text-xs font-semibold text-green">{activity.category}</span><span className="flex items-center gap-1 text-xs text-slate"><CalendarDays size={13}/>{formatDate(activity.date)}</span></div><h3 className="mt-4 text-xl font-semibold text-forest">{activity.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-slate">{activity.shortDescription}</p><Link href={`/kegiatan/${activity.slug}`} className="mt-5 inline-flex items-center gap-1 text-sm font-semibold text-green">Lihat dokumentasi <ArrowUpRight size={15}/></Link></div></article>}
